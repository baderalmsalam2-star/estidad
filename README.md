# منصة الاستعداد لاختبارات الوظائف الدينية

### وزارة الأوقاف والشؤون الإسلامية — دولة الكويت

تطبيقٌ يُدرّب المتقدّمين لوظائف **الإمامة** و**الأذان** على المنهج المقرَّر من إدارة
الشؤون الفنية بقطاع المساجد، ويبقى مرجعاً بعد التعيين.

**مجانيٌّ بالكامل — صدقةٌ جارية.** وهذا قيدٌ معماريٌّ لا شعار: التطبيق ملفاتٌ ساكنةٌ
بلا خادمٍ ولا قاعدة بيانات، فكلفة تشغيله صفر، ولا يتوقّف لانقطاع تمويل.

---

## التشغيل

```bash
npm start          # ثم افتح http://localhost:3000
```

لا اعتمادياتٍ ولا خطوةَ بناء. `tools/serve.js` خادمٌ ساكنٌ للتطوير فحسب؛ وللنشر
ارفع مجلّد المشروع كما هو على أي استضافةٍ ساكنة.

> لا يعمل بفتح `index.html` مباشرةً — الوحدات (ES modules) و`fetch` تحتاجان خادماً.
> ويحتاج التسجيل الصوتيّ اتصالاً آمناً (`https`) أو `localhost`.

---

## الشاشات العشر

بُنيت على اللغة البصرية **2a** من `شاشات التطبيق.dc.html`: أسطحٌ بحوافّ ٢٤–٢٨ بكسل
بلا ظلال، وشريط تنقّلٍ عائم، وأزرارٌ حبّةٌ كاملةٌ بارتفاع ٥٦ بكسل، وتدرّجٌ طباعيٌّ
على Amiri للعناوين وIBM Plex Sans Arabic للمتن.

| # | الشاشة | الملف |
|---|---|---|
| ٠١ | اختيار المسار | `screens/track.js` |
| ٠٢ | الرئيسية | `screens/home.js` |
| ٠٣ | الكتب ودراسة كتاب | `screens/books.js` |
| ٠٤ | السؤال المقاليّ | `screens/quiz.js` |
| ٠٥ | التصحيح الذاتيّ | `screens/quiz.js` |
| ٠٦ | الاختيار من متعدد | `screens/quiz.js` |
| ٠٧ | النتيجة | `screens/quiz.js` |
| ٠٨ | محرّك التجويد | `screens/tajweed.js` |
| ٠٩ | التسميع الصوتيّ | `screens/recite.js` |
| ١٠ | بطاقات الحفظ | `screens/flashcards.js` |

وزيادةً عليها: **اختبارٌ مخصّص** (`custom.js`)، و**حسابي** (`account.js`)، و**البحث في
المنهج** (`search.js`).

### وِرد اليوم

الاختبار موعدٌ لا يُؤجَّل، والدفعةُ اليوميةُ الصغيرةُ المتَّصلةُ أنفعُ من جلسةٍ واحدةٍ
طويلة. فبطاقةُ **وِرد اليوم** في الرئيسية تعرض ما أُنجِز اليوم من القَدر الملتزَم
(١٠ أو ٢٠ أو ٣٠ أو ٥٠، يُختار في «حسابي»)، وشريطَ الأيام السبعة، وسلسلةَ المواظبة.

وجلسةُ الوِرد ليست عشوائية: ثلثُها ممّا أخطأ فيه الطالب — فالخطأ أحقُّ بالمراجعة
من الجديد — وبقيّتُها ممّا لم يمرَّ عليه، ثمّ تُوزَّع على العلوم بالتناوب حتى لا
يقع الوِردُ كلُّه في علمٍ واحد. وبذرةُ الاختيار يومُ التقويم نفسُه، فالوِردُ ثابتٌ
ما دام اليومُ قائماً ولو أُغلِق التطبيقُ وعاد.

والسلسلةُ كلُّها مُشتَقّةٌ من طوابع `answers[].at` — لا سجلَّ زائداً يُحفَظ. وتُعَدُّ
رجوعاً من اليوم، فإن لم يُذاكِر اليومَ بعدُ بُدئ من أمسِ، وإلا انقطعت سلسلةُ من
يفتح التطبيق صباحاً قبل أن يبدأ.

### البحث في المنهج

تجاوز البنك ألفَي سؤال، فلم يعد التصفّح وحدَه يكفي. والبحث يُجرَّد من التشكيل
والهمزات — فمن كتب «الربا» أصاب «الرِّبا»، ومن كتب «الاخفاء» أصاب «الإخفاء» —
ويرتّب النتائج على موضع المطابقة: ما وقع في نصِّ السؤال أولى بالطالب مما وقع في
اسم بابه، وذاك أولى مما وقع في شرح الإجابة. ويُصيب رقمَ الصفحة أيضاً: «١٣٢» تردُّ
كلَّ سؤالٍ وُلِّد من تلك الصفحة.

وموضعُ المطابقة يُظلَّل على النصِّ **الأصليِّ بضبطه كما في الكتاب** لا على النصِّ
المجرَّد — وذلك يقتضي خريطةً تردُّ كلَّ حرفٍ مطبَّعٍ إلى موضعه في الأصل
(`normalizeWithMap`)، وإلا لانزاح التظليل بمقدار ما أُسقط من التشكيل.

---

## البنية

```
index.html
assets/
  css/app.css        ← رموز التصميم (2a) واللبنات
  css/fonts.css      ← الخطوط، مستضافةٌ محلياً
  fonts/             ← ٢٤ ملف woff2 (عربي/لاتيني فقط)
  js/
    app.js           ← نقطة الدخول وتسجيل الشاشات
    data.js          ← تحميل البنوك، والفلترة على المسار، وبناء الاختبارات
    store.js         ← التقدّم في localStorage
    audio.js         ← تسجيلات التسميع في IndexedDB
    ui.js            ← بناء DOM، والأرقام العربية، والملاحة
    screens/         ← شاشةٌ لكل ملف
data/
  manifest.json      ← المسارات الثلاثة وجدول البنوك
  banks/             ← ١٠ بنوك، ٤٤١ سؤالاً (منها ٥ مولَّدةٌ من صفحات الكتاب)
  tajweed/           ← ٥٦٤ آية × ٢٩٣٨ حكماً
books/               ← الكتب المقرَّرة PDF
  pages/             ← صور الصفحات المستشهَد بها (نحو ٥٠ ك.ب للصفحة)
docs/                ← حزمة المحتوى الأصلية ومواصفة التسميع
sources/             ← نصوص الكتب الستة (للمقابلة والبحث، لا للعرض)
tools/               ← الخادم الساكن، ومحرّك التجويد، وأداة استخراج نصّ PDF
```

**الخطوط مستضافةٌ محلياً** لا من Google Fonts: يعمل بلا اتصال، وبلا اعتمادٍ على
طرفٍ ثالثٍ قد ينقطع، وهي نفس ملفات التصميم بالضبط.

---

## قواعد لا تُخالَف — مطبَّقةٌ في الكود

**١) `verified` و`frequency` لا يُعرَضان للطالب.** فائدتهما الوحيدة ترجيحُ اختيار
أسئلة الاختبار الشامل، في `weightOf()` بـ`data.js`. لا شارةَ لهما في أي شاشة.

**٢) `bookPage` يُعرَض مع كل سؤالٍ موثَّق** — عبر `pageCite()` في `ui.js`. هذه ورقة
الثقة الوحيدة في تطبيقٍ فرديٍّ بلا جهة اعتماد.

**٣) التنبيه ظاهر:** الأسئلة اجتهادٌ تدريبيٌّ لا أسئلةُ اختباراتٍ رسمية — في شاشة
اختيار المسار وفي «حسابي».

**٤) المذهب الحنبليّ** في الفقه: الصواب ما في دليل الطالب.

**٥) لا تصحيحَ آليّاً للمقاليّ ولا للتلاوة.** المقاليّ يصحّحه الطالب على `keyPoints`،
والتسميع يوقظ الأذن ولا يحكم. وخطأ التطبيق في تصحيح التلاوة يُثبّت الخطأ في لسان
الطالب وهو يحسب نفسه أتقن.

**٦) شارة «لم يُقابَل» للمطوّر وحده.** تُفعَّل بـ`?dev=1` وتُطفَأ بـ`?dev=0`، وتظهر
على كل سؤالٍ لم يُقابَل حرفاً بحرفٍ على الكتاب بعد، ليُكمل صاحب المشروع المقابلة
على دفعات وهو يستعمل التطبيق. **لا يراها الطالب البتّة.**

**٧) صفحة الكتاب على بُعد ضغطة.** شارة الصفحة بجانب السؤال زرٌّ يفتح **صورة
الصفحة** في طبقةٍ فوق الشاشة. وهي طبقةٌ لا شاشةٌ بديلة عمداً: لو استُبدلت الشاشة
لضاع تأشير الطالب على نقاط التصحيح الذاتيّ.

**٨) الموثَّق والمرشَّح لا يتشابهان.** `bookPage` المقابَل يظهر بشارةٍ رمليةٍ
مصمتة، والترشيح الآليّ بشارةٍ مُنقَّطةٍ باهتةٍ منتهيةٍ بعلامة استفهام، وفي العارض
سطرٌ صريح: «ترشيحٌ آليّ — قد لا تكون هذه صفحته».

**٩) بطاقات الحفظ لا تُرقَّم بنودها** — لأن بعض `keyPoints` قيودٌ لا معدودات،
فترقيمها يناقض العدد المذكور ويعلّم الطالب خطأً. العدد وحده هو الحاكم.

---

## المسارات الثلاثة

الفروق بين الإمام والمؤذن ثلاثةٌ فقط: مقدار الحفظ، والنحو (للإمام وحده)، وفقه
المعاملات (للإمام وحده). وما عدا ذلك فالكتب والنطاق متطابقان.

| المسار | `tracks` | الحفظ | الأسئلة |
|---|---|---|---|
| الأئمة | `imam` | ٥ أجزاء منها عمّ | ٤٣٦ |
| المؤذنون | `muezzin` | ٣ أجزاء منها عمّ | ٣٥٦ |
| المتقاعدون | `muezzin_retired` | جزءان منهما عمّ | ٥٩ |

أول شاشةٍ في التطبيق اختيارُ المسار، وكل شيءٍ بعدها يُفلتَر بحقل `tracks`.

---

## حالة التوثيق — اقرأها قبل النشر

**الأسئلة كلّها مُخرَجةٌ من الكتب المقرَّرة**، لا مؤلَّفةٌ من معرفةٍ عامّة. وحقل
`bookVerified` لا يعني وجود أصلٍ لها في الكتاب من عدمه، بل يعني شيئاً أضيق:
**هل قوبِلت حرفاً بحرفٍ على نصّ الكتاب أم لم تُقابَل بعد.**

قوبِل منها ٤٤ سؤالاً، فاحتاج ٤١ تصحيحاً — أي **نسبة خطأ ٩٣٪ في الأسئلة العدديّة
الدقيقة**. والدلالة أن الاستخراج من الكتاب لا يكفي وحده حيث يكون العدد هو الجواب:
سقط **العَسَل** من الأموال الزكوية فصارت أربعةً وهي خمسة، وصارت شروط الصلاة ستةً
وهي تسعة. فالخلل في الدقّة العدديّة لا في المصدر.

والباقي — ٣٩٢ سؤالاً — مُخرَجٌ من الكتب كذلك ولم يُقابَل بعد. ونصوص الكتب في
`sources/` كاملةٌ وقابلةٌ للبحث، فالمراجعة الباقية **بحثٌ لا استخراج**.

وحالة الحديث (٦٥) وميثاق المسجد (٢٥) أوثق من غيرهما: مبنيّةٌ على **النصّ الكامل**
للأربعين النووية بشرحها ولوثيقة الميثاق الرسمية، و`bookVerified` فيها صفرٌ لأن
المقابلة لم تُسجَّل سؤالاً سؤالاً لا لأنّ مصدرها غير محدَّد.

وأحكام التجويد: **٢٩٣٦ حكماً `source: auto`** تحتاج مراجعة مُقرِئٍ مجازٍ قبل النشر،
وحكمان `manual` من نصّ غاية المريد ص١٠٠. وعلامات الوقف غير مدرَجة. والرواية:
**حفصٌ من طريق الشاطبية**.

نصّ القرآن من **Tanzil العثماني v1.0.2** ببصمة MD5 متحقَّقة. ⚠️ لا يُستبدَل بنصٍّ
مستخرَجٍ بالمسح الضوئيّ ولا مكتوبٍ يدوياً.

### الكتب داخل التطبيق

| الكتاب | PDF | صفحاتٌ مرسومة |
|---|:--:|---|
| دليل الطالب | ✓ ٥٫٤ م.ب | ٣٩ |
| غاية المريد | ✓ ٥٫٠ م.ب | ٢٧ |
| التحفة السنية | ✓ ٣٫١ م.ب | ٢٣ |
| ميثاق المسجد | ✓ ٢٫٣ م.ب | ١٦ |
| **بريق الجمان** | **✗ مرجع** | ٧٠ صفحةً مطلوبةً بلا ملفّ |
| **زبدة التفسير** | **✗ مرجع** | ١٣ صفحةً مطلوبةً بلا ملفّ |

الكتابان الأخيران (١٤٫٧ و٢٤٫٥ م.ب) تعذّر جلبهما: تجاوزا حدّ التنزيل، والشبكة
الخارجية محجوبةٌ بسياسة البيئة فلم تنفع نسخ `archive.org`. فيُضافان يدوياً إلى
`books/` باسمَي `bareeq-aljuman.pdf` و`zubdat-altafseer.pdf`، ثم:

```bash
python3 tools/رسم_الصفحات.py     # يرسم صفحاتهما تلقائياً
```

وإلى أن يُضافا، يُحال الطالب إلى **مرجعهما الرسميّ** من داخل التطبيق —
`data/book-links.json`، ومصدره `docs/منهج_الأئمة_روابط_الكتب.md`. وهو البديل
الذي أوصى به دليل الكتب أصلاً.

### توليدُ الأسئلة من الكتاب — ما نجح وما فشل

الفكرة: بدل أن يُكتَب السؤال ثم **يُبحَث** عن صفحته (فتصيب المطابقة ٨٧٪)، يُولَّد
السؤال **من الصفحة** — فتكون الصفحة مصدرَه لا تخمينَه، و`bookVerified` ثابتاً
بالبناء. وهذا يُلغي مشكلة المطابقة من أصلها.

**وقد جُرِّبت ثلاث طرقٍ فأسقط القياسُ اثنتين:**

| الطريقة | النتيجة |
|---|---|
| استخراجٌ آليٌّ بأنماطٍ من نصّ `sources/` | **فشل** — ٢٢ تعداداً في ٦ كتب، ونصُّها ممسوخ، وبريق الجمان وميثاق المسجد صفر |
| إعادة OCR بدقّةٍ أعلى (٤٠٠ و٥٠٠ نقطة) | **لم يحسّن** — بل النصّ الحاليّ أحياناً أدقّ. سقفُ tesseract العربيّ هو الحدّ |
| **قراءة صورة الصفحة** | **نجح** — تُقرأ الصفحة كما هي، بتشكيلها وحواشيها |

فسبب فشل الاستخراج الآليّ ليس المنهج بل **جودة النصّ**: «وهوأربعة» بلا فراغ،
و«كتاب الركاد» بدل «كتاب الزكاة». والأنماط تنكسر قبل أن تُلتقط.

والقراءة البصرية دقيقة لكنها **غير قابلة للأتمتة**: تحتاج قراءةً لا سكربتاً.
غير أن معدّلها ضُوعف أربعاً: `tools/تجهيز_الصفحات.py` يجمع **أربع صفحاتٍ في
لوحةٍ واحدة** مرتَّبةً من اليمين، وقد جُرِّبت فقُرئت بتشكيلها وحواشيها كاملةً.
فالكتب الأربعة (١٠٠١ صفحة) تصير نحو **٢٥٠ قراءة** لا ألفاً.

```bash
python3 tools/تجهيز_الصفحات.py daleel-altalib 40 60   # يُجهّز اللوحات
python3 tools/تجهيز_الصفحات.py --حالة                  # ما أُنجز من كل كتاب
```

والتقدّم مسجَّلٌ في `data/generation-progress.json` صفحةً صفحة، فالعمل **يُستأنف
من حيث وقف** ولا يُعاد.

وبنك `09_generated_fiqh.json` فيه الآن **عشرة أسئلة** من ص٣٣ و٣٦-٣٩ من دليل
الطالب، كلُّها `bookVerified` بالبناء.

**وفائدةٌ عرَضية:** القراءة البصرية صحّحت فحصاً آلياً كان مخطئاً. اتّهم الفحصُ
`VQ-SLT-022` بأنه يقول «أربعة» ويعدّ أربعة عشر — وصفحة ٣٣ تنصّ: «وَأَرْكَانُ
الصَّلَاةِ أَرْبَعَةَ عَشَرَ». فالسؤال صحيح، والفحصُ هو الذي أخطأ في قراءة العدد.

### هيكلة الإجابات — `tools/هيكلة_الإجابات.py`

يفصل بنود الجواب عن قيوده في حقلٍ **مضاف** اسمه `answer`، فيصير الترقيم آمناً
والفحصُ ممكناً. والقاعدة: **لا يُخمَّن** — لا يُفصَل قيدٌ إلا إذا استقام العدد
بفصله بالضبط.

**والحصيلة ضئيلةٌ عمداً: ٤ أسئلة من ٤٤١.** لأن البنوك ببساطة لا تحمل عدداً
يقرأه الحاسوب — ستة أسئلة فقط تصرّح بـ«العدد: كذا». وقراءة العدد من نصّ الإجابة
جُرِّبت فسقطت: «اثنا عشر» في التفسير تعني البروج لا عدد البنود.

فمن هذا يتبيّن أن الأسئلة المولَّدة من الصفحة أنظف بنيةً من أصلها، لأنها تُكتب
بالعدد والبنود مفصولين.

### اعتماد التوثيق — `?dev=1` ← حسابي ← «اعتماد التوثيق»

الغاية أن تُعتمد الأسئلة كلّها، ولا سبيل إلى ذلك آلياً: المطابقة أصابت ٨٧٪،
فواحدٌ من كل ثمانيةٍ يخطئ. فبُنيت شاشةٌ تجعل الاعتماد البشريّ سريعاً:

1. تعرض السؤال وإجابته وصفحته المرشَّحة في مكانٍ واحد، وضغطةٌ تفتح صورة الصفحة.
2. ترتّب الأسئلة فتقدّم **ما كانت إجابته عدداً** — مكمن ٩٣٪ من الأخطاء.
3. تنبّه صراحةً حين يذكر الجواب عدداً لا يظهر لفظُه في الصفحة المرشَّحة.
4. «اعتمد» أو «يحتاج تصحيحاً» أو «لاحقاً»، والقرارات تُحفظ محلياً.
5. «نزِّل ملفّ الاعتماد» ثم:

```bash
python3 tools/دمج_الاعتماد.py اعتماد-التوثيق.json           # عرضٌ فقط
python3 tools/دمج_الاعتماد.py اعتماد-التوثيق.json --apply   # يكتب bookVerified وbookPage
```

فلا يصير السؤال موثَّقاً إلا بقرار إنسانٍ نظر في الصفحة.

**لماذا صورةٌ للصفحة لا فتحٌ للكتاب؟** لأن فتح دليل الطالب يعني تنزيل ٥٫٤ ميغابايت
لرؤية صفحةٍ واحدة. أما الصورة فنحو ٥٠ كيلوبايت — أي **مئةُ ضعفٍ أسرع** على شبكة
الجوال. والكتاب كاملاً يبقى على بُعد ضغطةٍ من داخل العارض، ويُفتَح على الصفحة
نفسها بـ`#page=N`. وقد تُحقّق أن ترقيم الـPDF مطابقٌ لترقيم المطبوع في الكتب
الأربعة (إزاحة صفر)، فالرابط يقع في موضعه.

### ترشيح الصفحات — `tools/مقابلة.py`

يبحث عن كل سؤالٍ بلا `bookPage` في نصّ كتابه ويقترح أقرب صفحة، ويكتبها في
`data/page-hints.json`. **ولا يعدّل البنوك ولا يثبّت `bookVerified` البتّة.**

**دقّةٌ مقيسة** على الأسئلة الأربعة والأربعين المعروفة إجابتها (اختبارٌ محجوب):
**٣٧ من ٤٤ مطابقٌ تماماً**، ومن التي تتجاوز العتبة **٢٧ من ٣١ (٨٧٪)**. فواحدٌ من
كل ثمانيةٍ تقريباً يخطئ — ولذلك يُعرَض بشارةٍ مختلفة، معيناً على التنقّل لا شهادةَ
توثيق.

الحصيلة: **١٤٦ ترشيحاً** فوق العتبة، و١٨١ دونها، و٦٥ سؤالاً بلا كتابٍ نصّي
(الحديث).

### نصوص `sources/` — كيف تُستعمل

كل ملفٍ مقسَّمٌ بفواصل صفحاتٍ بهذه الصيغة، ورقمُها **رقم المطبوع** لا تسلسلاً:

```
==================== صفحة 103 ====================
```

فالبحث عن مسألةٍ ثم قراءة الفاصل قبلها يعطي `bookPage` مباشرةً. وترقيم زبدة
التفسير يبدأ من ٥٥٥ وينتهي بـ٦٠٤ لأن المقرَّر جزء عمّ في آخر الكتاب.

| الكتاب | الصفحات | المصدر |
|---|---:|---|
| دليل الطالب | ٣٩٦ | OCR — ترقيمٌ مطابقٌ ١:١ للمطبوع |
| غاية المريد | ٣٠٤ | OCR |
| بريق الجمان | ٢٨٣ | OCR — خطوط Type 3 بترميزٍ مخصَّص |
| التحفة السنية | ١٨٣ | OCR |
| زبدة التفسير | ٥٠ (ص٥٥٥–٦٠٤) | OCR |
| ميثاق المسجد | ١١٨ | `tools/استخراج_نص_pdf.py` |

⚠️ **الضجيج حقيقيّ.** عيّنةٌ من دليل الطالب ص٢٨: «وهي تسعه : الاسلام. والعقل»
**والتمير**”” » وكذا الطهاره مع **القدرق**». فالمطابقة الحرفية تفشل كثيراً،
والمطابقة بالكلمات المفتاحية بعد تجريد التشكيل تنجح في نحو ٨٠٪.

**قياسٌ فعليّ:** من الأسئلة الأربعة عشر الموثَّقة في دليل الطالب، وُجد **١١**
في صفحاتها المذكورة. ومن أسئلة الميثاق الخمسة والعشرين، رُشِّحت صفحةٌ بثقة ٥٠٪
فأكثر لـ**١٩**. فالبحث الآليّ **مُرشِّحٌ لا حَكَم** — يقترح الصفحة ويقصّ نصّها،
ويبقى إثبات `bookVerified` قراراً بشرياً على الـPDF.

---

## ما لم يُبنَ بعد

- **رفع التسجيلات لشيخ** — يُنزَّل الملف حالياً أو يُشارَك عبر `navigator.share`،
  ولا رفعَ إلى خادم. وحين يُبنى فليكن على Cloudflare R2 (الإخراج مجانيّ) مع حذفٍ
  تلقائيٍّ بعد ٣٠ يوماً، وإلا انهار شرط المجانية.
- **وحدة الأذان** — ألفاظه الخمس عشرة (وسبع عشرة للفجر بالتثويب) والإقامة بإحدى
  عشرة، على نصّ ميثاق المسجد (`07_meethaq.json`: MTH-012 و013 و014).
- **جدول استثناءات التجويد اليدويّ** — مواضع السكت، والإظهار المطلق، والإمالة،
  والتسهيل، والوقف اللازم. وهو **القيمة الحقيقية** لأنه ما لا يُولَّد آلياً.
- **بيانات الطالب محليّة فقط** ولا مزامنة بين الأجهزة.

---

## التطوير

تم تطوير التطبيق بواسطة **بدر المسلم** — والسطر ظاهرٌ صغيراً أسفل شاشات اختيار
المسار والرئيسية وحسابي، كما في رحلة المدينة والماهر.

---

## جهة الاتصال الرسمية

إدارة الشؤون الفنية — قطاع المساجد، هاتف **22262740**.
الرقعي، مبنى تدريب الأئمة والمؤذنين، الدور الثاني.
